<?php

use Symfony\Component\HttpFoundation\RedirectResponse;

function homebase_user_login($account) {
  $url = "/";
  $response = new RedirectResponse($url);
  $response->send();
  return;
}

function homebase_form_alter(&$form, \Drupal\Core\Form\FormStateInterface $form_state, $form_id){
  var_dump($form_id);
}
